let value;

const value1 = 10;
const value2 = 4;

// aritmetik opratörler

// value = value1 + value2; // toplama
// value = value1 - value2; // çıkarma 
// value = value1 * value2; // çarpma
// value = value1 / value2; // bölme
// value = value1 % value2; // mod


// math objesi

value = Math.PI; // pi sayısı
value = Math.E; // 2.71

value = Math.round(3.7); // sayı yuvarlama
value = Math.round(3.2); // sayı yuvarlama

value = Math.ceil(3.2); // yukarı yuvarlar
value = Math.floor(3.2); // aşağı yuvarlar

value = Math.sqrt(16); // karekök
value = Math.abs(-10); // mutlak değer

value = Math.pow(8,3); // üs alma 8in 3.kuvveti
value = Math.max(10,-1,30,100,98);
value = Math.min(10,-1,30,100,98);

value = Math.random(); // 0 -1 arası

value = Math.random()*20; // 0-20 arası
value = Math.random() *20 + 1; // 0-21 arası

value = Math.floor(Math.random() * 20 + 1);




console.log(value);